import { Options } from 'rotating-file-stream';
export interface PinoRotatingFileStreamOptions extends Options {
    filename: string;
    path: string;
}
declare const _default: (options: PinoRotatingFileStreamOptions) => import("rotating-file-stream").RotatingFileStream;
export default _default;
